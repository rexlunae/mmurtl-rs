/// Placeholder build script — actual image creation is done via Makefile
fn main() {
    // We just need this to exist so cargo recognizes the build-dependency
    // configuration. Image creation is handled by the Makefile.
}
