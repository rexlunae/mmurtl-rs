[unstable]
build-std = ["core", "compiler_builtins", "alloc"]
build-std-features = ["compiler-builtins-mem"]

[build]
target = "x86_64-unknown-none.json"

# Required for JSON target specs on nightly
# We handle this via Makefile / cargo args instead
